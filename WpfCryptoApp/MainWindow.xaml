<Window x:Class="WpfCryptoApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfCryptoApp"
        xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF" 
        xmlns:materialIcons="clr-namespace:Material.Icons.WPF;assembly=Material.Icons.WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="12*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="12*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Column="1" Grid.Row="1">
            <Frame x:Name="TopCoinsFrame" NavigationUIVisibility="Hidden" />
        </Grid>

            <Button Content="{materialIcons:MaterialIconExt Kind=Home}" 
                    Command="{Binding ReturnHomeCommand}"
                   Grid.Column="0" Grid.Row="0" FontSize="14" Margin="10,5,10,5"/>

            <Grid Grid.Row="0" Grid.Column="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0" 
             Name="SearchTextBox" 
             Text="Enter coin id"
             Background="Transparent"
             FontSize="16" FontWeight="Bold"
             VerticalContentAlignment="Center"/>

            <Button Grid.Column="1" 
            Name="SearchButton" 
            Content="{materialIcons:MaterialIconExt Kind=Search}"
            FontSize="12" 
            Command="{Binding GetInfoCommand}"/>

            <Button Grid.Column="2" 
            Name="ChartButton"
            FontSize="12" 
            Content="{materialIcons:MaterialIconExt Kind=BarChart}" 
            Command="{Binding SearchCommand}">
            </Button>
        </Grid>
    </Grid>


</Window>
